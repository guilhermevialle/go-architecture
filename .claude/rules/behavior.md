---
paths:
  - '**/*.go'
---

- Never comment in codes
- Never run project commands e.g. `go mod tidy`, `god mod init`, etc
- Always explicit declare types correctly
- Organize code like pieces, cannot tangle/suffocate lines e.g. `blank line between code blocks is a good thin and makes it more organized` 